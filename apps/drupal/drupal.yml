x-default-opts: &default-opts
  env_file:
    .env
  logging:
    options:
      max-size: "1m"
    # driver: "gelf"
    # options:
    #   gelf-address: "udp://172.17.0.1:12201"

services:
  web:
    <<: *default-opts
    image: drupal:${DRUPAL_TAG}
    networks:
      - proxy_network
      - drupal_network
    volumes:
      - data:/var/www/html
    environment:
      - DRUPAL_DB_HOST=db
      - DRUPAL_DB_NAME=${DB_NAME}
      - DRUPAL_DB_USER=${DB_USER}
      - DRUPAL_DB_PASSWORD=${DB_PASSWORD}
      - DRUPAL_DB_DRIVER=mysql
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
      labels:
        traefik.enable: "true"
        traefik.http.routers.drupal.entrypoints: ${ENTRYPOINT:-websecure}
        traefik.http.routers.drupal.rule: Host(`${DOMAIN?Variable not set}`)
        traefik.http.routers.drupal.service: drupal
        traefik.http.services.drupal.loadbalancer.server.port: 80

  db:
    <<: *default-opts
    image: mariadb:${MARIADB_TAG}
    networks:
      - drupal_network
    volumes:
      - db:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=${DB_NAME}
      - MYSQL_USER=${DB_USER}
      - MYSQL_PASSWORD=${DB_PASSWORD}
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure

  backup:
    <<: *default-opts
    image: mariadb:${MARIADB_TAG}
    command: 
      - sh
      - -c
      - | 
        echo "[$$(date +'%Y-%m-%d %H:%M:%S')] Backup service started. First backup will be executed in $$BACKUP_FREQUENCY.";
        while true; do
          backup_dir=/backup/drupal_$$(date +'%Y%m%d_%H%M%S');
          mkdir -p $$backup_dir;
          echo "[$$(date +'%Y-%m-%d %H:%M:%S')] Creating backup of Drupal data including SQL dump of $$DB_NAME database...";
          /usr/bin/mariadb-dump -u $$DB_USER --password=$$DB_PASSWORD --host=db --databases $$DB_NAME --no-tablespaces > $$backup_dir/db.sql;
          tar -C /data/ -cvzf $$backup_dir/data.tar.gz .;
          find /backup/drupal_* -mtime +$$RETENTION_DAYS -exec rm -rf {} \;
          echo "[$$(date +'%Y-%m-%d %H:%M:%S')] Backup completed. Next backup in $$BACKUP_FREQUENCY.";
          sleep $$BACKUP_FREQUENCY;
        done
    volumes:
      - backup:/backup
      - data:/data
    networks:
      - drupal_network
    deploy:
      restart_policy:
        condition: on-failure
        delay: 600s

networks:
  proxy_network:
    external: true
    
  drupal_network:
    driver: overlay

volumes:
  data:
    driver: local
    driver_opts:
      device: ${HOST_DRUPAL_DATA}
      type: none
      o: bind
      # device: ":${HOST_DRUPAL_DATA?Variable not set}"
      # type: nfs
      # o: addr=${NFS_SHARE_IP?Variable not set},rw,sync,nfsvers=4.1

  db:
    driver: local
    driver_opts:
      device: ${HOST_DRUPAL_DB}
      type: none
      o: bind
      # device: ":${HOST_DRUPAL_DB?Variable not set}"
      # type: nfs
      # o: addr=${NFS_SHARE_IP?Variable not set},rw,sync,nfsvers=4.1

  backup:
    driver: local
    driver_opts:
      device: ${HOST_DRUPAL_BACKUP}
      type: none
      o: bind
      # device: ":${HOST_DRUPAL_BACKUP?Variable not set}"
      # type: nfs
      # o: addr=${NFS_SHARE_IP?Variable not set},rw,sync,nfsvers=4.1
